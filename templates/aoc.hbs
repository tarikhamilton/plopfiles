import fs from 'fs'
import path from 'path'

const input = fs.readFileSync(path.resolve(__dirname, 'input'), 'utf-8').trim()

const partOne = () => input

const partTwo = () => input

export default [partOne(), partTwo()]